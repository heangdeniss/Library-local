<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Analysis and Statistics</title>
</head>
<body>
    <h1><%= title %> - Data Analysis & Statistics</h1>
    <h2>Author: <%= author %></h2>

    <% if (filePath) { %>
        <!-- Display PDF using the file path -->
        <object class="pdf-object" data="<%= filePath %>" type="application/pdf" width="100%" height="600px">
            <p>Your browser does not support PDFs. 
                <a href="<%= filePath %>">Download the PDF</a>.
            </p>
        </object>
    <% } else { %>
        <p>No PDF found.</p>
    <% } %>
</body>
</html>
